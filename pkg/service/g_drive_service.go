package service

func GetDriveDirectories() {

	// List all directories (folders) on Google Drive
	// driveFiles, err := driveSrv.Files.List().Q("mimeType='application/vnd.google-apps.folder'").Do()
	// if err != nil {
	// 	log.Fatalf("Unable to retrieve folders: %v", err)
	// }

	// // Print the names and IDs of the directories
	// fmt.Println("Directories on Google Drive:")
	// for _, folder := range driveFiles.Files {
	// 	fmt.Printf("Name: %s, ID: %s\n", folder.Name, folder.Id)
	// }
}
